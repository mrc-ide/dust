# -*- makefile -*-
# Based on similar approach in redux developed by Jeroen
PKG_CFLAGS=-DHAVE_INLINE $(SHLIB_OPENMP_CFLAGS) -I../windows/gsl-2.4/include -I../inst/include
PKG_LIBS= -L../windows/gsl-2.4/lib${R_ARCH} -lgsl $(BLAS_LIBS) $(SHLIB_OPENMP_CFLAGS)

all: clean winlibs

winlibs:
	"${R_HOME}/bin${R_ARCH_BIN}/Rscript.exe" "../tools/winlibs.R"

clean:
	rm -f $(SHLIB) $(OBJECTS)
