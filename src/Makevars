PKG_CFLAGS=-Itrng -DHAVE_INLINE $(SHLIB_OPENMP_CFLAGS)
PKG_CXXFLAGS=-Itrng -DHAVE_INLINE $(SHLIB_OPENMP_CXXFLAGS)
PKG_LIBS=$(BLAS_LIBS) $(SHLIB_OPENMP_CFLAGS)
SOURCES=$(wildcard *.cpp *.c trng/*.cc)
OBJECTS=$(SOURCES:.cpp=.o) $(SOURCES:.c=.o) $(SOURCES:.cc=.o) 